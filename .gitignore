# Python
__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/
build/
dist/
*.egg-info/
.pytest_temp/

# Environments
.venv/
venv/

# Local config
.env
.env.*
!.env.example

# Local editor/agent state (personal config, not project code)
.codex/
.vscode/
.gemini/
.continue/
.claude-plugin/
.claude/

# Runtime artifacts (generated at server startup)
.codegraph/
.agent-context/
.pytest_temp/

# Workspace plans (ephemeral)
.omo/

# IDE rule symlinks (auto-generated by installer, same content as AGENTS.md)
.clinerules
.copilotrules
.cursorrules

# OpenCode local config
.opencode/package.json
.opencode/package-lock.json
.opencode/node_modules/

# RTK subproject — strip nested .git if accidentally cloned
rtk/.git/

